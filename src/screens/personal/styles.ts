import { StyleSheet, ViewStyle } from "react-native";

export default () => {
  return StyleSheet.create({
    root: {
      flex: 1,
      alignItems: 'center',
      paddingTop: 20,
      paddingBottom: 20,
      gap: 40,
    },
  });
}
